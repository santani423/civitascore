<?xml version="1.0" encoding="utf-8"?>
<!-- Mengizinkan HTTP polos hanya ke alias localhost emulator Android (BAB 13.2,
     `API_BASE_URL` default dev `http://10.0.2.2:8000`) — backend staging/prod
     wajib HTTPS dan tetap tunduk pada base-config default (cleartext ditolak). -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
